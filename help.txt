for (int i = 0; i < sizeOfList - 1; i++)
        {
            shortestTask = tempTask;
            for (int j = i + 1; j < sizeOfList; j++)
            {
                if (tempNode->next->task->burst < shortestTask->burst)
                {
                    shortestTask = tempNode->next->task;
                }
                if (shortestTask != tempTask)
                {
                    swap(shortestTask)
                }
            }
        }